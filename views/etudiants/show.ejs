<!DOCTYPE html>
<html lang="fr">
<head>
  <%- include('../partials/head') %>
  <title>Détails de l'Étudiant</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body class="bg-light">
  <%- include('../partials/navbar') %>
  
  <div class="container mt-5">
    <h1 class="text-center text-primary mb-4">Détails de l'Étudiant</h1>
    
    <div class="card shadow-sm rounded bg-white">
      <div class="card-body">
        <h5 class="card-title text-center text-info"><%= etudiant.prenom %> <%= etudiant.nom %></h5>
        
        <div class="row">
          <div class="col-md-6">
            <p class="card-text"><strong>Matricule:</strong> <%= etudiant.matricule %></p>
            <p class="card-text"><strong>Email:</strong> <%= etudiant.email %></p>
            <p class="card-text"><strong>Téléphone:</strong> <%= etudiant.telephone || 'Non renseigné' %></p>
          </div>
          <div class="col-md-6">
            <p class="card-text"><strong>Date d'inscription:</strong> <%= etudiant.dateInscription.toLocaleDateString() %></p>
          </div>
        </div>
        
        <div class="mt-4 text-center">
          <a href="/etudiants/<%= etudiant._id %>/edit" class="btn btn-warning btn-sm">
            <i class="bi bi-pencil-square"></i> Modifier
          </a>
          <form action="/etudiants/<%= etudiant._id %>?_method=DELETE" method="POST" class="d-inline">
            <button class="btn btn-danger btn-sm" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet étudiant?')">
              <i class="bi bi-trash"></i> Supprimer
            </button>
          </form>
          <a href="/etudiants" class="btn btn-secondary btn-sm">
            <i class="bi bi-arrow-left-circle"></i> Retour à la liste
          </a>
        </div>
      </div>
    </div>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
